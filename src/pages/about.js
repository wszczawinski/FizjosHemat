import React from 'react';

import { Layout } from '../components';

const AboutPage = () => {
    return (
        <Layout>
            <h1>About page</h1>
        </Layout>
    );
};

export default AboutPage;
