import React from 'react';
import Header from '../components/Header';

const blogPage = () => {
    return (
        <div>
            <Header />
            <h1>Blog page</h1>
        </div>
    );
};

export default blogPage;
