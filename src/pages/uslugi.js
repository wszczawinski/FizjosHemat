import React from 'react';
import Layout from '../components/Layout';

const methodsPage = () => {
    return (
        <Layout>
            <h1>Blog page</h1>
        </Layout>
    );
};

export default methodsPage;
