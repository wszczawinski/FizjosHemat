import React from 'react';

import Layout from '../components/Layout';

const Blog = () => {
    return (
        <Layout>
            This is a blog template
        </Layout>
    )
};

export default Blog